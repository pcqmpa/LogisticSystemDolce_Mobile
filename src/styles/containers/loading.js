/**
 * Module with the Loading component styles.
 * @module src/styles/containers/loading
 */
// React Native.
import { StyleSheet } from 'react-native';

const loadingStyles = StyleSheet.create({
  container: {
    flex: 1
  }
});

export default loadingStyles;
