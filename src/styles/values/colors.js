/**
 * Module with the app Colors.
 * @module src/styles/values/colors
 */
const white = '#FFFFFF';

const black = '#111111';

const brand = '#2c3e50';

const greyDarker = '#7B9298';

const greyAlpha = 'rgba(0, 0, 0, 0.85)';

export default {
  white,
  black,
  brand,
  greyDarker,
  greyAlpha
};
