/**
 * Module with the Spinner text theme.
 * @module src/styles/themes/spinner-text
 */
// Values.
import { colors } from '../values/';

const spinnerText = {
  color: colors.brand
};

export default spinnerText;
