/**
 * Module with the app style themes.
 * @module src/styles/themes
 */
// React Native.
import { StyleSheet } from 'react-native';

// Styles.
import spinnerText from './spinner-text';

const themes = StyleSheet.create({
  spinnerText
});

export default themes;
