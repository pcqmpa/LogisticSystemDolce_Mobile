/**
 * Module with the scene component styles.
 * @module src/styles/components/scene
 */
// React Native.
import { StyleSheet } from 'react-native';

const scene = StyleSheet.create({
  container: {
    opacity: 0
  }
});

export default scene;
