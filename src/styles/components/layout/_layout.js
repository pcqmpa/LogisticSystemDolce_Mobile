/**
 * Module with the layout component styles.
 * @module src/styles/components/layout/_layout
 */
// React Native.
import { StyleSheet } from 'react-native';

// Styles.
import { toast, toastText } from './toast';

export default StyleSheet.create({
  toast,
  toastText
});
