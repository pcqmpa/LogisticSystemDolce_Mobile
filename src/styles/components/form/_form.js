/**
 * Module with the form component styles.
 * @module src/styles/components/form/_form
 */
// React Native.
import { StyleSheet } from 'react-native';

// Styles.
import {
  textForm,
  textFormContainer,
  validIconTextForm
} from './text-form';
import { button, buttonText } from './button';

export default StyleSheet.create({
  button,
  buttonText,
  textForm,
  textFormContainer,
  validIconTextForm
});
