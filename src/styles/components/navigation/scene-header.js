/**
 * Module with the SceneHeader component styles.
 * @module src/styles/components/navigation/scene-header
 */
// Values.
import { colors, variables } from '../../values';

export const sceneHeader = {
  height: variables.sceneHeader.height,
  justifyContent: 'center',
  alignItems: 'center',
  backgroundColor: colors.greyDarker
};

export const sceneHeaderLabel = {
  width: 200,
  fontSize: 20,
  textAlign: 'center'
};
