/**
 * Module with the navigation components styles.
 * @module src/styles/components/navigation/_navigation
 */
// React Native.
import { StyleSheet } from 'react-native';

// Styles.
import {
  sceneHeader,
  sceneHeaderLabel
} from './scene-header';

export default StyleSheet.create({
  sceneHeader,
  sceneHeaderLabel
});
