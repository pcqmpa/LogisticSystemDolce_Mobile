/**
 * Module with the Main container styles.
 * @module src/containters/Main/styles
 */
// React.
import { StyleSheet } from 'react-native';

// Constants.
import { FLEX_FIT } from '../../constants/values';

const styles = StyleSheet.create({
  mainContainer: {
    flex: FLEX_FIT
  }
});

export default styles;
